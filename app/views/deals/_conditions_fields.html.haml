.control-group
    .controls= condition.text_area :description, :size => '10x5'

.control-group
    .controls
        = condition.text_field :type

.control-group
    .controls
        = condition.link_to_remove :class => "btn btn-warning", :id => "appendedInputButton" do
            %i.icon-remove
